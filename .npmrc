# pnpm v11 runs a dependency-integrity check before scripts. esbuild/spawn-sync
# have prebuilt binaries and don't need their build scripts, but the check still
# blocks commands. Disable the pre-run check so build/dev/test work non-interactively.
verify-deps-before-run=false
